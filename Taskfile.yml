---
version: "3"

vars:
  PROJECT_DIR:
    sh: "git rev-parse --show-toplevel"
  CLUSTER_DIR: "{{.PROJECT_DIR}}/cluster"
  ANSIBLE_DIR: "{{.PROJECT_DIR}}/provisioning/ansible"
  TERRAFORM_DIR: "{{.PROJECT_DIR}}/provisioning/terraform"

env:
  KUBECONFIG: "{{.PROJECT_DIR}}/provisioning/kubeconfig"

includes:
  # ansible: .taskfiles/ansible.yml
  flux: .taskfiles/flux.yml
  format: .taskfiles/format.yml
  lint: .taskfiles/lint.yml
  pre-commit: .taskfiles/pre-commit.yml
  terraform: .taskfiles/terraform.yml
  ansible: .taskfiles/ansible.yml
