---
- name: "Copy Kodi systemd"
  template:
    src: "kodi.service.j2"
    dest: "/lib/systemd/system/kodi.service"
    mode: "0644"
  notify: "Restart Kodi"

- name: "Start/Enable Kodi"
  systemd:
    name: "kodi"
    state: "started"
    enabled: yes
    daemon_reload: yes
    # scope: "user"
