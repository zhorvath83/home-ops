---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:

  sync:
    desc: Sync repo Codex skills and rules into ${CODEX_HOME}
    cmds:
      - /bin/zsh {{.ROOT_DIR}}/.taskfiles/Codex/sync-links.sh
