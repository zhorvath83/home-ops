# Trash
.DS_Store
Thumbs.db
# Binaries
bin
/flux
*.iso
# Temp folders
.temp*
.private/
.logs/
.task/
# Log files
*.log
# Ansible
xanmanning.k3s*
mrlesmithjr.zfs*
geerlingguy.docker*
geerlingguy.pip*
# Terraform
.terraform
.terraform.tfstate*
terraform.tfstate*
# Sops
.decrypted~*
*.asc
*gpg*
# Kubernetes
kubeconfig*
talosconfig*
# Defense in depth — never commit raw certs/keys. SOPS-encrypted versions
# use distinct extensions (e.g. *.sops.yaml), so they are not affected.
*.crt
*.key
# Folder Structure v3
/root
megalinter-reports/
-e
# Python
__pycache__/
*.pyc
*.pyo
.claude/*
!.claude/CLAUDE.md
!.claude/settings.json
!.claude/skills/
.claude/settings.local.json
.mcp.json
