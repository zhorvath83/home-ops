---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: urlwatch-data
  namespace: selfhosted
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: rook-ceph-block
