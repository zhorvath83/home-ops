---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: wikijs-config
    namespace: default
    files:
      - config.yaml
generatorOptions:
  disableNameSuffixHash: true
