---
apiVersion: v1
kind: Service
metadata:
  name: ttrss
  namespace: default
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: ttrss
  type: ClusterIP
