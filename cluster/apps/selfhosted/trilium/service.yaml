---
apiVersion: v1
kind: Service
metadata:
  name: &app trilium
  namespace: selfhosted
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: *app
