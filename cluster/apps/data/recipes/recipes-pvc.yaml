---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recipes-media-data
  namespace: data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: rook-ceph-block
