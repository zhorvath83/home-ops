---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: private-website
  namespace: cloud
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: networking-middleware-chain-noauth@kubernetescrd,cloud-middleware-redirect-from-www@kubernetescrd
spec:
  ingressClassName: "traefik"
  rules:
  - host: "${SECRET_CLUSTER_DOMAIN}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: private-website
            port:
              number: 80
  - host: "www.${SECRET_CLUSTER_DOMAIN}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: private-website
            port:
              number: 80

---

apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: middleware-redirect-from-www
  namespace: cloud
spec:
  redirectRegex:
    regex: ^https?://(www.)?${SECRET_CLUSTER_DOMAIN}/(.*)
    replacement: https://${SECRET_CLUSTER_DOMAIN}/$${2}
    permanent: true
