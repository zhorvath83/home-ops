---
apiVersion: v1
kind: Service
metadata:
  name: home-assistant-web
  namespace: default
spec:
  type: ClusterIP
  externalTrafficPolicy: Local
  ports:
    - name: home-assistant-web
      protocol: TCP
      port: 80
      targetPort: 8123
  selector:
    app.kubernetes.io/name: pihole
