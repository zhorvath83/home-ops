---
apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: default
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8123
  selector:
    app.kubernetes.io/name: home-assistant
