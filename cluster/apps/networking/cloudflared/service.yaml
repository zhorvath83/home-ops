---
apiVersion: v1
kind: Service
metadata:
  name: cloudflared
  namespace: networking
spec:
  ports:
    - protocol: TCP
      name: web
      port: 443
      targetPort: 5053
  selector:
    app.kubernetes.io/name: cloudflared
