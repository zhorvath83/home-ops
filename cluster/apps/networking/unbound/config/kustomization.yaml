---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - unbound-config.yaml
configMapGenerator:
  - name: unbound-root-hints
    namespace: default
    files:
      - root.hints
generatorOptions:
  disableNameSuffixHash: true
