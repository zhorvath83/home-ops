---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: middleware-cf-access-auth
  namespace: networking
spec:
  forwardAuth:
    address: "http://traefik-auth-cloudflare.networking.svc.cluster.local/auth/${SECRET_CF_ACCESS_APP_AUD_TAG}"
    trustForwardHeader: true
