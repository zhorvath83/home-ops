---
apiVersion: v1
kind: Service
metadata:
  name: traefik-error-pages
  namespace: networking
spec:
  ports:
    - protocol: TCP
      name: web
      port: 8080
  selector:
    app.kubernetes.io/name: traefik-error-pages
