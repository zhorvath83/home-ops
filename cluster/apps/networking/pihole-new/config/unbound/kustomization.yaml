---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: unbound-config
    namespace: default
    files:
      - unbound.conf
  - name: unbound-root-hints
    namespace: default
    files:
      - root.hints
generatorOptions:
  disableNameSuffixHash: true
