---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: cloudflare-ddns
  namespace: networking
  labels:
    app.kubernetes.io/name: cloudflare-ddns
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflare-ddns
  strategy:
    # All existing Pods are killed before new ones are created
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflare-ddns
    spec:
      containers:
      - name: cloudflare-ddns
        image: ghcr.io/hotio/cloudflareddns@sha256:1ef69f9854654bb3ee71a41ac1835cef53e1c90cd77482609cc78d7b426d3e0d
        imagePullPolicy: IfNotPresent
        env:
        - name: DETECTION_MODE
          value: "dig-whoami.cloudflare"
        - name: CF_APITOKEN
          value: "${SECRET_CF_APITOKEN}"
        - name: CF_ZONES
          value: "${SECRET_CF_ZONES}"
        - name: CF_HOSTS
          value: "${SECRET_DOMAIN}"
        - name: CF_RECORDTYPES
          value: "${SECRET_CF_RECORDTYPES}"
        - name: LOG_LEVEL
          value: "1"
        - name: INTERVAL
          value: "60"
        resources:
          requests:
            cpu: 10m
            memory: 15Mi
          limits:
            cpu: 100m
            memory: 100Mi
