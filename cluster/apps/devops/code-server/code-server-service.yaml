---
apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: devops
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8443
  selector:
    app.kubernetes.io/name: code-server
  type: LoadBalancer
  loadBalancerIP: ${CLUSTER_SVC_CODE_SERVER_IP}
