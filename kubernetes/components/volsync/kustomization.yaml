---
# yaml-language-server: $schema=https://json.schemastore.org/component.json
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./externalsecret.yaml
  - ./pvc.yaml
  # Enable for the next cluster recreate to bootstrap freshly created PVCs
  # from VolSync via dataSourceRef -> ReplicationDestination.
  # - ./replicationdestination.yaml
  - ./replicationsource.yaml
