---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pingvin-share-x
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    controllers:
      pingvin-share-x:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          config-init:
            image:
              repository: public.ecr.aws/docker/library/bash
              tag: "5.3.15@sha256:a25cef5ed010cc5cdb115d4176a60231466d3989811eb6d7080d882e8d953401"
            command:
              - /usr/local/bin/bash
              - -c
              - envsubst < /config-template/config.yaml > /config/config.yaml
            envFrom:
              - secretRef:
                  name: pingvin-share-x-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
        containers:
          app:
            image:
              repository: ghcr.io/smp46/pingvin-share-x
              tag: v1.19.1@sha256:332f0b41acf1828f0f0221acb74304f6e1d33a7a952a1be60b3988eeae2b797e
            env:
              TZ: ${TIMEZONE}
              CADDY_DISABLED: "true"
              TRUST_PROXY: "true"
              DATA_DIRECTORY: /data
              CONFIG_FILE: /config/config.yaml
              DATABASE_URL: file:/data/pingvin-share.db?connection_limit=1
              PV_LOG_LEVEL: verbose
            probes:
              startup:
                enabled: true
                spec:
                  httpGet:
                    path: /api/health
                    port: 8080
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 30
              liveness:
                enabled: true
                spec:
                  httpGet:
                    path: /api/health
                    port: 8080
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness:
                enabled: true
                spec:
                  httpGet:
                    path: /api/health
                    port: 8080
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 1Gi
    service:
      app:
        controller: pingvin-share-x
        ports:
          http:
            port: &frontendPort 3333
          api:
            port: &backendPort 8080
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: Pingvin Share
          gethomepage.dev/group: Selfhosted
          gethomepage.dev/icon: pingvin-share
        hostnames:
          - "share.${PUBLIC_DOMAIN}"
        parentRefs:
          - name: envoy-external
            namespace: networking
            sectionName: https
          - name: envoy-internal
            namespace: networking
            sectionName: https
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /api
            backendRefs:
              - identifier: app
                port: *backendPort
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - identifier: app
                port: *frontendPort
    persistence:
      config-template:
        type: configMap
        name: pingvin-share-x-config
        advancedMounts:
          pingvin-share-x:
            config-init:
              - path: /config-template
      config:
        type: emptyDir
        advancedMounts:
          pingvin-share-x:
            config-init:
              - path: /config
            app:
              - path: /config
      data:
        existingClaim: pingvin-share-x
        advancedMounts:
          pingvin-share-x:
            app:
              - path: /data
      frontend-img:
        existingClaim: pingvin-share-x
        advancedMounts:
          pingvin-share-x:
            app:
              - path: /opt/app/frontend/public/img
                subPath: images
