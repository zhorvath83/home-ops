---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      # AD-023 vocabulary: envoy-internal only (ingress-from-gateway-internal CCNP) + internet egress via the allow-world-egress CCNP,
      # needed for metadata providers and the plugin catalog / Introskipper downloads (same class as plex and the *arr apps).
      labels:
        ingress.home.arpa/allow-gateway-internal: "true"
        egress.home.arpa/allow-world: "true"
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: "${APP}-gpu"
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        # The introskipper subPath mount under /config/data makes kubelet create that parent
        # root-owned; OnRootMismatch skips it (volume root gid already matches fsGroup) and
        # Jellyfin's /config/data/.jellyfin-data sanity marker fails. Always chmods it writable.
        # debt: recursive chown per restart; move to a dedicated data PVC if startup slows
        fsGroupChangePolicy: Always
        seccompProfile:
          type: RuntimeDefault
    controllers:
      jellyfin:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/jellyfin/jellyfin
              tag: 10.11.11@sha256:45f648c382a0c8b552582fcea40e95cb17c5d475473a891cba0eb7523fb92112
            env:
              # .NET enforces FileShare with flock(), which is unreliable on the NFS media mount.
              DOTNET_SYSTEM_IO_DISABLEFILELOCKING: "true"
              JELLYFIN_PublishedServerUrl: "https://jellyfin.${PUBLIC_DOMAIN}"
            securityContext:
              privileged: false
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: &port 8096
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness: *probes
              startup:
                enabled: false
            resources:
              claims:
                - name: gpu
              requests:
                cpu: 25m
                memory: 512Mi
              limits:
                memory: 4Gi
    service:
      app:
        controller: jellyfin
        ports:
          http:
            port: *port
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: Jellyfin
          gethomepage.dev/group: Media
          gethomepage.dev/icon: jellyfin.svg
        hostnames:
          - "jellyfin.${PUBLIC_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: networking
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      config:
        type: persistentVolumeClaim
        existingClaim: jellyfin
        globalMounts:
          - path: /config
      # Rebuildable, deliberately NOT backed up: library artwork/metadata (point the
      # Jellyfin metadata path at /metadata in the UI) and the Introskipper fingerprint DB.
      metadata:
        existingClaim: jellyfin-metadata
        advancedMounts:
          jellyfin:
            app:
              - path: /metadata
                subPath: metadata
              - path: /config/data/introskipper
                subPath: introskipper
      nfs-nas-media:
        type: nfs
        server: ${NAS_IP}
        path: /media
        globalMounts:
          - path: /media
      # JELLYFIN_CACHE_DIR=/cache also holds the transcode working directory.
      cache:
        type: emptyDir
      tmp:
        type: emptyDir
