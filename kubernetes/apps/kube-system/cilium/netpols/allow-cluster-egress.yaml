---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cilium.io/ciliumclusterwidenetworkpolicy_v2.json
#
# Cluster-wide baseline egress policy (bjw-s-labs pattern).
#
# Default model: every pod without the opt-out label receives a broad egress
# allowance to other cluster endpoints and to the public internet. This makes
# `CiliumNetworkPolicy` opt-in for restrictions instead of forcing every pod
# to repeat DNS / cluster / world egress rules.
#
# Opt-out: label a pod or workload with
#   egress.home.arpa/custom-egress: ""
# to remove it from this baseline. Any per-app CNP attached to that pod then
# becomes the sole source of egress allow rules — and the pod falls back to
# default-deny for any egress not explicitly listed there.
#
# Companion policy: `allow-dns-egress` (applies to ALL pods, including opted-
# out ones; DNS allowance via L7 proxy is a separate concern).
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: allow-cluster-egress
spec:
  endpointSelector:
    matchExpressions:
      - key: egress.home.arpa/custom-egress
        operator: DoesNotExist
  egress:
    - toEndpoints:
        - {}
    - toEntities:
        - cluster
    - toEntities:
        - world
