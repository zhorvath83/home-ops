---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/tuppr.home-operations.com/talosupgrade_v1alpha1.json
apiVersion: tuppr.home-operations.com/v1alpha1
kind: TalosUpgrade
metadata:
  name: talos
spec:
  talos:
    # renovate: datasource=custom.talos-factory depName=siderolabs/talos
    version: "v1.13.6"
  # Skip the kubectl drain step: on a single-node cluster there is nowhere to drain.
  drain:
    enabled: false
  policy:
    rebootMode: powercycle
  healthChecks:
    - description: "All ReplicationSources in idle condition"
      apiVersion: volsync.backube/v1alpha1
      kind: ReplicationSource
      expr: |-
        status.conditions.filter(c, c.type == "Synchronizing").all(c, c.status == "False")
