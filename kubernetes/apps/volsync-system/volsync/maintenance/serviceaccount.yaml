---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kopia-maintenance
# Kopia maintenance jobs talk to OVH S3 + the Kopia repo (auth from volsync-secret),
# never the K8s API — do not mount a ServiceAccount token.
automountServiceAccountToken: false
