---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex-imagemaid
  namespace: default
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: plex-imagemaid

  install:
    createNamespace: true
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  uninstall:
    keepHistory: false

  values:
    defaultPodOptions:
      automountServiceAccountToken: true
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/name
                    operator: In
                    values: ["plex"]
              topologyKey: kubernetes.io/hostname

    controllers:
      plex-imagemaid:
        annotations:
          reloader.stakater.com/auto: "true"
        serviceAccount:
          identifier: plex-imagemaid
        type: cronjob
        cronjob:
          schedule: "30 7 * * *"
          timeZone: Europe/Budapest
          concurrencyPolicy: Forbid

        containers:
          plex-control:
            image:
              repository: docker.io/alpine/k8s
              tag: 1.35.2@sha256:9a87b09e1b6d887fa52c2dea1611d112d9e7d3d0271708588711a2fb2016e82c
            command:
              - /bin/sh
              - -c
              - |
                set -eu

                echo "Reading current Plex replica count"
                ORIGINAL_REPLICAS="$(kubectl get deployment plex --namespace default --output jsonpath='{.spec.replicas}')"
                if [ -z "${ORIGINAL_REPLICAS}" ]; then
                  ORIGINAL_REPLICAS="1"
                fi

                echo "${ORIGINAL_REPLICAS}" > /shared/original_replicas

                if [ "${ORIGINAL_REPLICAS}" != "0" ]; then
                  echo "Scaling Plex down"
                  kubectl scale deployment plex --namespace default --replicas=0
                  while kubectl get pods --namespace default --selector app.kubernetes.io/name=plex --output name | grep -q .; do
                    sleep 5
                  done
                fi

                touch /shared/plex-stopped

                while [ ! -f /shared/imagemaid.done ] && [ ! -f /shared/imagemaid.failed ]; do
                  sleep 5
                done

                if [ "${ORIGINAL_REPLICAS}" != "0" ]; then
                  echo "Scaling Plex up"
                  kubectl scale deployment plex --namespace default --replicas="${ORIGINAL_REPLICAS}"
                  kubectl rollout status deployment/plex --namespace default --timeout=10m
                fi

                if [ -f /shared/imagemaid.failed ]; then
                  exit 1
                fi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
                memory: 30Mi
              limits:
                memory: 64Mi

          app:
            image:
              repository: docker.io/kometateam/imagemaid
              tag: v1.1.1@sha256:fbec331b36c1473f8a9c4214f55e5b097860476c8a5274eb61a2b85df89e56cc
            command:
              - /bin/sh
              - -c
              - |
                set -eu

                while [ ! -f /shared/plex-stopped ]; do
                  sleep 2
                done

                echo "Running ImageMaid"
                if python3 /imagemaid.py; then
                  touch /shared/imagemaid.done
                else
                  touch /shared/imagemaid.failed
                  exit 1
                fi

            env:
              TZ: Europe/Budapest
              PLEX_PATH: /data/plex_config
              MODE: report
              LOCAL_DB: "true"
              IGNORE_RUNNING: "false"
              PHOTO_TRANSCODER: "true"

            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: ["ALL"] }

            resources:
              requests:
                cpu: 25m
                memory: 128Mi
              limits:
                memory: 512Mi

    persistence:
      plex:
        existingClaim: plex
        globalMounts:
          - path: /data/plex_config

      plex-cache:
        existingClaim: plex-cache
        globalMounts:
          - path: /data/plex_config/Cache

      shared:
        type: emptyDir
        globalMounts:
          - path: /shared

      config:
        type: emptyDir
        globalMounts:
          - path: /config

      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp

    serviceAccount:
      plex-imagemaid: {}

    rbac:
      roles:
        plex-imagemaid:
          enabled: true
          type: Role
          rules:
            - apiGroups: [""]
              resources: ["pods"]
              verbs: ["get", "list"]
            - apiGroups: ["apps"]
              resources: ["deployments"]
              verbs: ["get", "patch"]
      bindings:
        plex-imagemaid:
          enabled: true
          type: RoleBinding
          roleRef:
            identifier: plex-imagemaid
          subjects:
            - kind: ServiceAccount
              name: plex-imagemaid
              namespace: "{{ .Release.Namespace }}"

    service:
      app:
        controller: plex-imagemaid
        enabled: false
