---
# Shared PVC for Plex rebuildable data:
# cache, metadata artwork, and scanner assets.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-rebuildable
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: democratic-csi-local-hostpath
