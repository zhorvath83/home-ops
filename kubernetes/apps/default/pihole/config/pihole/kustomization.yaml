---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pihole-env-vars.yaml
configMapGenerator:
  - name: pihole-config
    namespace: default
    files:
      - adlists.list
      - custom.list
      - 10-subnet-dns.conf
generatorOptions:
  disableNameSuffixHash: true
