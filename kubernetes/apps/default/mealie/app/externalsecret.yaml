---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mealie
  namespace: default
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: mealie-secret
    creationPolicy: Owner
    template:
      data:
        # smtp-relay
        SMTP_FROM_EMAIL: "{{ .SMTP_RELAY_USERNAME }}"
        OPENAI_BASE_URL: "{{ .OPENROUTER_CHAT_BASE_URL }}"
        OPENAI_MODEL: "{{ .OPENROUTER_MODEL }}"
        OPENAI_API_KEY: "{{ .OPENROUTER_API_KEY }}"
  dataFrom:
    - extract:
        key: smtp-relay
    - extract:
        key: openrouter
