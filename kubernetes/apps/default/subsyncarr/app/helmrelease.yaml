---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: subsyncarr
  namespace: default
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: subsyncarr

  install:
    createNamespace: true
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  uninstall:
    keepHistory: false

  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: ${APP_UID}
        runAsGroup: ${APP_GID}
        fsGroup: ${APP_GID}
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault

    controllers:
      subsyncarr:
        type: deployment

        containers:
          app:
            image:
              # https://github.com/tomtomwillis/subsyncarr_plus
              repository: tomtomw123/subsyncarr-plus
              tag: 1.2.5@sha256:df361f4e14fe34c85c37d20612c03454dc975425ae3e17913b2f80060799b226

            env:
              TZ: "Europe/Budapest"
              PUID: "${APP_UID}"
              PGID: "${APP_GID}"
              CRON_SCHEDULE: "disabled"
              SCAN_PATHS: "/media"
              MAX_CONCURRENT_SYNC_TASKS: "1"
              INCLUDE_ENGINES: "ffsubsync,autosubsync"
              WEB_HOST: "0.0.0.0"
              WEB_PORT: "3000"

            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: &port 3000
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness: *probes
              startup:
                enabled: true
                custom: true
                spec:
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
                  tcpSocket:
                    port: *port

            securityContext:
              privileged: false
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }

            resources:
              requests:
                cpu: 5m
                memory: 30Mi
              limits:
                memory: 1Gi

    service:
      app:
        controller: subsyncarr
        ports:
          http:
            port: *port

    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: Subsyncarr
          gethomepage.dev/group: Arr Stack
          gethomepage.dev/icon: mdi-subtitles
        hostnames:
          - "subsync.${PUBLIC_DOMAIN}"
        parentRefs:
          - name: envoy-external
            namespace: networking
        rules:
          - backendRefs:
              - identifier: app
                port: *port

    persistence:
      data:
        type: emptyDir
        globalMounts:
          - path: /app/data
      media:
        type: nfs
        server: ${CONF_NFS_SRV_IP}
        path: /media/shows
        globalMounts:
          - path: /media
