---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: database
resources: []
  # Future backup-related resources can be added here
  # Valkey backup is currently handled via volsync in the app directory
